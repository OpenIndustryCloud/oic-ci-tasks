---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: gcr.io/landg-179815/fission-cli
    tag: 'latest'

inputs:
- name: task-src

params: 
  FUNCTION_ENVIRONMENT: staging
  FUNCTION_IMAGE: fission/go-env:latest
  # BUILDER_IMAGE: fission/go-env:builder
  FISSION_ROUTER: fissionrouter.default.svc.cluster.local
  FISSION_URL: python

run:
  path: task-src/env/update/update_env.sh
